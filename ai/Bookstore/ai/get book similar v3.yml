info:
  name: get book similar v3
  type: http

http:
  method: GET
  url: http://localhost:8000/recommendations/v3/books/1/similar?limit=10
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
