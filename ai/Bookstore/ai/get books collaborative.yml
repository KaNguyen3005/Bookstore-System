info:
  name: get books collaborative
  type: http

http:
  method: GET
  url: http://localhost:8000/recommendations/v3/users/1/collaborative?limit=10
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
