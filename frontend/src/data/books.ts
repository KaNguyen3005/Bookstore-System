export interface Book {
  id: number
  title: string
  price: number
  author: string
}


{/*import { books } from "../data/books";

   function Home() {
     return (
       <div>
         {books.map(book => (
           <p key={book.id}>{book.title}</p>
         ))}
       </div>
     );
   }*/}